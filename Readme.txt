Created UI in react using react lifecycle and router .Also can be made available by passing the username and password in header of particular report data.

for eg:http://localhost:3000/reportData?un=xip&ps=xip123